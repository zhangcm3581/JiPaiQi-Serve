# 验证记录

日期：2026-09-09。环境：本机 macOS，Python 3.14.6，FastAPI 0.141.1，Uvicorn 0.52.4，SQLite，真实浏览器和本机 WebSocket 连接。

## 自动测试

最新完整报告：`reports/test-report.html`（通过`.venv/bin/python scripts/test_report.py`生成）。本次 **72项通过**，包括原32项业务与协议测试、9项部署更新测试、8项版本边界回归、23项真实HTTP/WebSocket场景。总耗时约83秒。

本次发现并修复1个服务端问题：版本查询缺少范围校验，超大整数导致SQLite溢出，HTTP请求异常或WS连接退出。修复前8项边界回归失败；`Store.round()`统一校验后全部通过。

新增真实网络模拟：20局随机牌组、12轮第7份与结束竞争、8租户56连接、强制终止后恢复、应用心跳与45秒空闲回收。共发送829条测试消息，收到7249条服务端消息。8租户并发场景最后一份到各租户七端结果的最大观测延迟20.78毫秒。每个测试数据库完整性、外键与手牌唯一性检查通过。

具体场景、预期和实际证据见HTML报告，重跑方法见[测试说明](testing.md)。以下保留各层测试覆盖和早期页面检查记录。

覆盖内容：

- 两个租户可使用相同版本，数字字符串的前导零保留。
- 每份严格13张，允许双副牌相同牌出现两次；累计第三张拒绝，事务回滚。
- 7个设备91张输入与剩余13张的完整点数、花色、多重集相符；6份不生成结果。
- 同请求重放、同手牌不同顺序重传、相同内容不同设备、不同内容复用请求ID。
- 结束消息重复、上一局迟到数据、未绑定设备、跳过多个版本、同一新局事件重绑。
- 首份有效数据启动固定计时，重复消息不续期，重启恢复截止时间，超时后推进一次。
- 第7份手牌和结束并发、7个重复提交并发、重复结束并发。
- 历史手牌、结果和历史绑定版本不会随下一局变化。
- 手动版本推进、禁止回退、停用/启用、设备登记上限、移除登记不破坏历史。
- 版本整数到达上限时停用该租户，不绕回、不阻塞其他租户的超时处理。
- HTTP校验、空值、消息尺寸、Origin；WebSocket连接身份一致性、异常消息、二进制帧。
- 管理页面收到7份完整手牌；普通设备状态消息不带其他客户端手牌。
- WebSocket重连、旧连接被新连接替代、在线状态、服务启动时超时恢复。
- 今日指标使用北京时间零点。

当前测试依赖有两条上游弃用提示（Starlette/httpx、AnyIO别名），不影响通过结果。未隐藏提示。

`ruff check app tests scripts` 与 `ruff format --check app tests scripts` 通过。GitHub Actions 已配置 Ubuntu 24.04 / Python 3.12，但本次没有推送或运行远端 CI。

## 更新脚本验证

`scripts/update.sh`通过`bash -n`语法检查。9项更新测试使用临时本地Git仓库和真实SQLite备份，隔离模拟root身份、systemd、依赖安装和健康检查；未操作实际服务或生产数据库。

- 成功更新：快进当前分支、保留完整数据库备份、安装新依赖并重启。
- 依赖安装失败、健康检查失败：恢复原提交、原虚拟环境和服务；不覆盖数据库。
- 没有新提交：不重启服务，不重复创建备份。
- 已跟踪文件改动、未跟踪文件、分支分叉：更新前拒绝，不覆盖本地内容。
- 数据库备份失败：保持原代码和依赖，重新启动原服务。
- 存在另一个更新任务：拒绝并发执行。
- 备份目录保持私有权限，更新后的代码和依赖可被服务用户读取。

尚未在真实Ubuntu主机执行GitHub拉取和systemd更新；首次部署及更新步骤见部署文档。

## 真实连接联调

运行 `scripts/simulate.py`，使用独立测试租户900001与7条实际WebSocket连接，连续两局，并追加一轮重启恢复验证。

- 第7份提交到7个客户端全部收到正确结果：三次分别约12.3、6.1、7.6毫秒。
- UI在6/7时显示等待点动画；最后一份到达无需刷新，网格填入13张。
- 收齐后保持同一版本，结束后进入下一版本，历史可展开查看真实数据。
- 终止并重新启动Uvicorn，SQLite中的已收手牌与结果保留，后台重新连接成功。

这是本机回环网络结果，不代表手机网络或大量租户下的延迟；APK协议改造及7台模拟器实测尚未进行。

## 页面检查

- 按已确认样式拆分4个Jinja页面、公共模板、CSS、ES模块；实时与历史共用手牌/结果组件。
- 937×810窗口下工作台无横向溢出，7行手牌及结果可同时查看。
- 1071×669窗口下总览中的7行手牌和结果网格同屏可见。
- 历史展开显示正确的双副牌第二行，网格无顶部数字表头，A到K列固定。
- 创建租户默认版本1；备注、版本及设备列表回填来自服务端。
- 编辑备注期间服务端版本由4变5，保存备注成功且未回退或覆盖服务端版本。
- 页面检查期间无JavaScript控制台错误。

浏览器使用的数据在本机 `data/jpq.sqlite3`，已被Git忽略；部署不附带联调数据。后端没有在真实Ubuntu服务器部署，部署步骤和配置文件已准备好。
